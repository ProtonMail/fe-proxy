{
  "name": "fe-proxy",
  "version": "1.0.0",
  "description": "A simple proxy server that redirects to different urls.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "ProtonMail",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.0"
  }
}
