{
  "name": "fe-proxy",
  "version": "1.0.0",
  "description": "A simple proxy server that redirects to different urls.",
  "main": "index.js",
  "scripts": {
    "start": "node index --route='^/settings/ http://localhost:8081' --route='http://localhost:8080' --port=8040"
  },
  "keywords": [],
  "author": "ProtonMail",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.0"
  }
}
